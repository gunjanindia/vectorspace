import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth";import {db} from "@/lib/prisma";
export async function POST(req:Request,{params}:{params:Promise<{id:string;moduleId:string}>}){const user=await getCurrentUser();if(!user||user.role!=="ADMIN")return NextResponse.json({error:"Unauthorized"},{status:401});const{moduleId}=await params;const b=await req.json();const count=await db.lesson.count({where:{moduleId}});const lesson=await db.lesson.create({data:{moduleId,title:b.title,type:b.type||"VIDEO",durationMin:Number(b.durationMin)||0,videoUrl:b.videoUrl||null,content:b.content||null,sortOrder:count+1}});return NextResponse.json({lesson})}
