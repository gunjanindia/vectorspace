import {NextResponse} from "next/server";import {getCurrentUser} from "@/lib/auth";import {db} from "@/lib/prisma";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentUser();if(!user||user.role!=="ADMIN")return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const b=await req.json();const count=await db.courseModule.count({where:{courseId:id}});const module=await db.courseModule.create({data:{courseId:id,title:b.title,sortOrder:count+1},include:{lessons:true}});return NextResponse.json({module})}
