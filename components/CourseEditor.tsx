"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import RichTextEditor from "./RichTextEditor";
type Instructor={id:string;name:string;email:string};
export default function CourseEditor({instructors}:{instructors:Instructor[]}){const router=useRouter();const[saving,setSaving]=useState(false);const[error,setError]=useState("");const[form,setForm]=useState({title:"",slug:"",shortDescription:"",description:"",level:"Beginner",durationHours:"10",price:"0",mode:"ONLINE",instructorId:instructors[0]?.id||"",featured:false,published:false});const update=(k:string,v:string|boolean)=>setForm(f=>({...f,[k]:v}));async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);setError("");try{const r=await fetch("/api/admin/courses",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.error||"Could not save course");router.push(`/admin/courses/${d.id}`)}catch(e){setError(e instanceof Error?e.message:"Could not save course")}finally{setSaving(false)}}return <form className="form" onSubmit={submit} style={{marginTop:20}}>{error&&<p className="error">{error}</p>}<label>Course title<input className="input" value={form.title} onChange={e=>update("title",e.target.value)} required/></label><label>Slug<input className="input" value={form.slug} onChange={e=>update("slug",e.target.value)} placeholder="auto-generated if blank"/></label><label>Short description<RichTextEditor value={form.shortDescription} onChange={v=>update("shortDescription",v)} placeholder="Write a concise formatted course summary..." minHeight={110}/></label><label>Full description<RichTextEditor value={form.description} onChange={v=>update("description",v)} placeholder="Write the complete course description with headings, lists and emphasis..." minHeight={220}/></label><div className="grid grid-2"><label>Level<input className="input" value={form.level} onChange={e=>update("level",e.target.value)} required/></label><label>Duration (hours)<input className="input" type="number" min="0" value={form.durationHours} onChange={e=>update("durationHours",e.target.value)} required/></label></div><div className="grid grid-2"><label>Price (₹)<input className="input" type="number" min="0" value={form.price} onChange={e=>update("price",e.target.value)} required/></label><label>Mode<select className="input" value={form.mode} onChange={e=>update("mode",e.target.value)}><option>ONLINE</option><option>OFFLINE</option><option>HYBRID</option><option>SELF_PACED</option></select></label></div><label>Instructor<select className="input" value={form.instructorId} onChange={e=>update("instructorId",e.target.value)} required>{instructors.map(i=><option key={i.id} value={i.id}>{i.name} ({i.email})</option>)}</select></label><label><input type="checkbox" checked={form.featured} onChange={e=>update("featured",e.target.checked)}/> Featured course</label><label><input type="checkbox" checked={form.published} onChange={e=>update("published",e.target.checked)}/> Publish immediately</label><div style={{display:"flex",gap:10,marginTop:20}}><button className="btn btn-primary" disabled={saving}>{saving?"Saving…":"Create Course"}</button><button type="button" className="btn btn-secondary" onClick={()=>router.push("/admin/courses")}>Cancel</button></div></form>}
